<h1>Totp#edit</h1>

<p>
<%= flash[:warn] %>
</p>

<%= @qr %>

<%= form_with(model: @totp, local: true, url: totp_index_path(id: @user.id)) do |form| %>
  <%= form.hidden_field :secret_key %>
  <div class="field">
    <%= form.label :otp %>
    <%= form.text_field :otp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Back', root_path %>
